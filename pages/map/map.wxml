<view>
  <map
    id="myMap"
    class="map"
    latitude="{{latitude}}"
    longitude="{{longitude}}"
    scale="4"
    min-scale="3"
    max-scale="10"
    enable-poi="{{false}}"
    markers="{{markers}}"
  ></map>
</view>
