<view class="container">
      <view class="box">
        <qiun-wx-ucharts 
          type="line"
          opts="{{opts}}"
          chartData="{{chartData}}"
          background="#000814"
          disableScroll="{{true}}"
          onzoom="{{true}}"
          ontouch="{{true}}"
        />
      </view>
    <view class="bottom">
      <view class="tag" bind:tap="toDataManagement">数据管理</view>
      <view class="tag" bind:tap="toMap">地图</view>
    </view>
</view>