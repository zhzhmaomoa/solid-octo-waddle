<view class="container">
  <scroll-view scroll-x="true">
    <view class="box">
      <qiun-wx-ucharts 
        type="line"
        opts="{{opts}}"
        chartData="{{chartData}}"
        onzoom="true"
        ontouch="true"
        background="#000814"
      />
    </view>
  </scroll-view>
  <view class="bottom">
    <view class="tag" bind:tap="toDataManagement">数据管理</view>
  </view>
</view>