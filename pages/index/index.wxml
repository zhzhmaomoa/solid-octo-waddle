<view class="page">
  <image 
    wx:for="{{imgs}}"
    wx:key="index"
    class="img"
    src="{{item.src}}"
    data-index="{{index}}" 
    bindload="handleImgLoaded" 
    style="grid-column-end: span {{imgsWidth[index]}}"
  ></image>
</view>
